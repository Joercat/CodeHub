{
  "name": "CodeHub",
  "version": "1.0.0",
  "description": "A comprehensive AI coding assistant platform with multiple Hugging Face models",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node -e \"const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('./ai_chat.db'); console.log('Database initialized'); db.close();\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ai",
    "coding",
    "assistant",
    "huggingface",
    "nodejs",
    "express",
    "sqlite"
  ],
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "node-fetch": "^3.3.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "dotenv": "^16.3.1",
    "morgan": "^1.10.0",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
}
